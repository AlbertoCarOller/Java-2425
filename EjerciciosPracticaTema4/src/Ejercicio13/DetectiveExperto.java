package Ejercicio13;

import java.time.LocalDate;

public class DetectiveExperto extends Detective {

    // Creamos el constructor
    public DetectiveExperto(String nombre, LocalDate fechaIngreso) {
        super(nombre, fechaIngreso);
    }
}