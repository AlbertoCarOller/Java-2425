package Ejercicio13;

import java.time.LocalDate;

public class DetectivePrincipiante extends Detective {

    // Creamos el constructor
    public DetectivePrincipiante(String nombre, LocalDate fechaIngreso) {
        super(nombre, fechaIngreso);
    }
}